# Pink Wardrobe — Cloud Functions (Python, 2nd gen) dependencies.
#
# DEPLOYMENT NOTE: rembg (onnxruntime) and mediapipe are both large wheels.
# Together with opencv this bundle lands around 700MB-1GB unpacked, which is
# why the pipeline functions are configured with 2GB memory in main.py.
# See docs/DEPLOYMENT.md for the model-caching strategy that keeps cold
# starts from re-downloading the u2net weights on every invocation.

firebase-functions~=0.4.2
firebase-admin~=6.5.0

# Segmentation
rembg==2.0.57
onnxruntime==1.18.1

# Pose landmarks
mediapipe==0.10.14

# Poisson blending (seamlessClone) + warping
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# FASHN HTTP client
requests==2.32.3
